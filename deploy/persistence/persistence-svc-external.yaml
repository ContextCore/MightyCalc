apiVersion: v1
kind: Service
metadata:
  name: persistence-svc-external
  labels:
    app: persistence-svc-external
spec:
  type: NodePort
  ports:
  - port: 5432
    name: postgres
  selector:
    app: mightycalc-persistence-statefulset